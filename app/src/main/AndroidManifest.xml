<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="app.hackathon.pragyan">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PlaceOrder.PlaceOrderView"></activity>
        <activity android:name=".Payment.PaymentView" />
        <activity android:name=".Slot.SlotView" />
        <activity android:name=".Register.RegisterView" />
        <activity android:name=".Login.LoginView" />
        <activity android:name=".OrderView.OrderView" />
        <activity android:name=".Status.StatusView" />
        <activity android:name=".Feedback.FeedbackView" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>